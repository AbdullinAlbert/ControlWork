<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.albertabdullin.controlwork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ListOfEmplActivity"
            android:label="Список сотрудников"
            android:parentActivityName="com.albertabdullin.controlwork.FillNewData_Activity">
        </activity>
        <activity
            android:name=".FillNewData_Activity"
            android:label="Добавьте результат работы"
            android:parentActivityName="com.albertabdullin.controlwork.MainActivity"
            android:windowSoftInputMode="stateHidden"
            />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>